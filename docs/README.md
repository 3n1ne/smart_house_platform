# 文档索引

文档已收敛为三个稳定入口：

- `api-design.md`：REST API 契约、权限规则、请求参数和响应字段。
- `database-design.md`：核心表结构、实体关系和推荐索引。
- `deployment.md`：环境变量、迁移、管理员初始化、前端构建、运维脚本、安全要求、性能与兼容性验收。

原始需求保留在根目录 `requirement.md`。项目运行入口和当前状态见根目录 `README.md`。
